var tweets_loaded = "<%= @tweets_loaded %>";
var total_tweets = "<%= @total_tweets %>";
updateLoadingBarProgress(tweets_loaded, total_tweets);
<% if @done %>
	finalize();
<% else %>
	incrementalLoadTweets(200); 
<% end %> 
//all functions called from here live in sessions.js
