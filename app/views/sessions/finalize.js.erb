$(".progress").fadeOut("slow", fadeInResults);

function fadeInResults(){
 	$("#results").append("<%= escape_javascript(render(:partial => 'layouts/results')) %>");
 	<% if not @is_user_on_own_page %>
		$("#tweet_form").hide();
	<% end %>
	$("#results").fadeIn('fast');
}
